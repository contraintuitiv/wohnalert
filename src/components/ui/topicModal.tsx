export function topicModal() {
    return (
        <div className="fixed z-50 items-center justify-center">
            <div className="bg-white p-6 w-full max-w-md flex">
                {/* Modal Header */}
                {/* I jsut need one field to enter a name for a topic, which then will be send back to the parent component */}
            </div>
        </div>
    );
}
